// TypeScript type definitions
// Place all shared types here

// Re-export from utils/types for now (will be consolidated later)
export type {
  Creator,
  Project,
  Product,
  Job,
  Show,
  Chat,
} from '../src/utils/types';

