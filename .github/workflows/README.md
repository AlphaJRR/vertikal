# GitHub Actions Workflows

## Valid Workflows

- `deploy-cloudflare.yml` - Standard Cloudflare Pages deployment
- `cloudflare-advanced-deploy.yml` - Advanced canary/blue-green deployment

## Note

The `main.yml` file was invalid (contained shell commands instead of YAML) and has been removed.

