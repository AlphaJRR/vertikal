/**
 * P0 kill-switch — disable all Home HLS/native video mounts until cold-start is stable.
 * Set false once VideoModal + deferred mount are verified on device.
 */
export const DISABLE_HOME_VIDEOS = true;
